{
  "name": "dzergen",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm run build-client && node ./bin/www",
    "start-backup": "node ./bin/www",
    "start-local": "npm run build-client && PORT=8080 node server",
    "build-client": "cd client && npm install && npm run build && cd ..",
    "build-client-backup": "cd client && npm run build && cd .."
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "jade": "^1.11.0",
    "morgan": "~1.9.1",
    "nodemon": "^1.19.1"
  }
}
